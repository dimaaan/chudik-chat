<?xml version="1.0" encoding="utf-8"?>
<!--
	Пути для ReceivedFilesProvider. Корень внешнего хранилища целиком, а не
	Download/ChudikChat: иначе здесь было бы второе место с путём из
	PlatformEnvironment.DownloadRoot, и разойдись они — файл перестал бы открываться
	без единой ошибки при сборке. Под этот же корень попадает и запасная папка
	приложения в Android/data, куда уходит принятое при отказе в разрешении на 8–10.

	Широта корня ничего не открывает: поставщик не экспортирован, и чужое
	приложение читает только выданный ему адрес.
-->
<paths>
	<external-path name="storage" path="." />
</paths>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!--
		requestLegacyExternalStorage нужен одному Android 10: там прямые пути в общее
		хранилище закрыты даже с разрешением на запись. С Android 11 флаг игнорируется.
	-->
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true" android:requestLegacyExternalStorage="true"></application>

	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

	<!--
		Без CHANGE_WIFI_MULTICAST_STATE нельзя взять MulticastLock, а без него
		Wi-Fi-радио отфильтровывает многоадресные кадры и телефон молча никого не видит.
		Это же разрешение служит предусловием для службы типа connectedDevice.
	-->
	<uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

	<uses-permission android:name="android.permission.WAKE_LOCK" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

	<!--
		Запись в «Загрузки» на Android 8–10. С Android 11 в Download/ пишут без
		разрешений, поэтому выше 29-го оно не запрашивается вовсе.
	-->
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
</manifest>

<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!--
      Состояние движка принадлежит циклу событий, и наружу оно не выставляется.
      Тестам всё же нужно спросить его о себе — отсюда internal-доступ.
    -->
    <InternalsVisibleTo Include="ChudikChat.Core.Tests" />
  </ItemGroup>

</Project>
